# pngshot Rust 重构 — 项目启动提示词

> 用途：把这份文档整体粘贴给负责重构的 AI（如 Claude Code）作为第一条消息， 让它在新仓库里直接开始工作，而不是重新调研需求。

------

## 0. 你要做什么

把现有的 **pngshot**（Python + GTK4 + GObject Introspection，面向 Arch Linux / Wayland / niri 的截图工具）**用 Rust 完整重写**。这不是原型或 demo，是要接管一个 日常使用中的工具，因此：

1. **功能不能少、不能变行为**：下面列出的每一项现有功能都要在 Rust 版本里等价实现， 而不是"重新设计一遍"。
2. **UI 要重新设计得更精致**，但要保留现有的视觉语言（深色半透明、蓝色主动作、 紧凑不臃肿），不要做成移动端卡片堆叠或营销页风格。
3. **快捷键响应速度是硬指标**：从用户按下快捷键到覆盖层出现在屏幕上，要比 Python 版本明显更快，目标是感知上"零延迟"。
4. **长截图的效率和效果要更上一层楼**：采样帧率、拼接吞吐、匹配鲁棒性、内存占用 都要比 Python+OpenCV 版本更好，而不是原样照搬算法。

在开始写任何代码之前，先完整阅读随附的原项目源码（`pngshot/` 目录）和本文件， 理解现有实现后再动手，**不要凭猜测重新发明功能**。

------

## 1. 运行环境（硬约束，不可更改）

- 目标平台：**Arch Linux + Wayland + niri 合成器**，不追求跨平台、不做 X11 兼容层。
- 快捷键完全由 **niri 的 `binds {}`** 管理，pngshot 进程本身**不监听全局键盘**， 只提供一个命令行/socket 入口被 niri 调用。
- 不做"大型客户端"：常驻部分只允许一个轻量系统托盘 + 一个后台控制服务， 其余全是按需拉起的一次性进程/窗口。
- 长截图依赖用户手动滚动：Wayland 不允许普通应用安全地合成滚轮事件， **不要承诺或实现"自动滚动"**，这是设计事实，不是待办事项。
- 截图控制条、选区边界等 UI 元素**绝对不能进入最终产出的图片**。

------

## 2. 现有功能清单（重写后必须全部保留）

### 2.1 区域截图

- 全屏 layer-shell 覆盖层，拖拽框选，之后可移动/调整选区大小。
- 工具栏贴近选区显示（不是固定在屏幕角落），需要适配窄选区和 niri 的 1/3、1/2、2/3 分栏宽度，不能被裁切或错位。
- 选区确认后：保存到 `~/Pictures/Screenshots`、复制到剪贴板（两者可在托盘里 分别开关），文件名格式类似 `pngshot-YYYY-MM-DD_HH-MM-SS-ffffff.png`。

### 2.2 标注

- 画笔、箭头、矩形、文字四种工具，颜色、粗细可各自选择，支持撤销。
- 标注状态下不能被 idle/焦点看门狗误关闭（Python 版本这里踩过坑，见第 7 节）。

### 2.3 OCR

- 默认本地 **Tesseract**（`chi_sim+eng`），根据选区宽高比自动选择单行/稀疏/ 块状版面模式。
- 预处理：灰度化、深色背景自动反色、autocontrast、放大到约 300 DPI（默认 3x）。 **不要对所有图片强制二值化/锐化**，会损伤干净背景下的小字识别率。
- 杂乱或半透明背景走另一条预处理路径：形态学估计背景、消除纹理后再 Otsu 二值化。
- 可选"视觉 OCR"：调用外部 LLM/VLM（当前是 opencode 视觉模型），用于小字、 低对比度场景；失败自动回退本地 Tesseract，不能让用户"什么都拿不到"。
- 结果展示为可编辑文本窗口。

### 2.4 翻译

- 先 OCR 得到文本，再调用配置的 LLM 做翻译。
- 优先探测并复用本机常驻的 `opencode serve --pure --port 47823` （OpenAI 兼容 HTTP API），减少每次翻译的冷启动；探测失败自动回退到一次性 CLI 调用（`opencode run`）。
- 已经是目标语言的文本要短路，不再送模型。
- 结果窗口要能看出当前走的是常驻服务还是 CLI 回退（简洁提示，不做长篇说明）。

### 2.5 长截图（重点，见第 4 节详细算法要求）

- 半自动流程：用户手动滚动目标窗口，pngshot 连续抓帧、去重、拼接。
- 交互：第一次按快捷键选区并开始采集；再次按同一快捷键完成当前长截图； 面板聚焦时 Enter 完成、Esc 取消。
- 只做垂直滚动，选区宽度不变；支持连续向下、向上、往返滚动。
- 自动识别吸顶导航栏、吸底栏、固定侧栏（连续贴边、经过至少三次滚动变化确认 的固定区域），拼接时屏蔽这些区域的匹配干扰，最终只保留一次。
- 视频、加载动画、闪烁光标等局部动态内容不能拖垮整体匹配，需要二级鲁棒匹配路径。
- 半透明窗口（终端、编辑器等）背后固定墙纸的拼接接缝需要羽化处理，避免硬切横线， 同时前景文字/UI 保持清晰、不产生动画残影。
- 完成时会做一次基于关键帧的全局路径校正，修复漏桥接、重复段、偶发错位； 无法验证完整路径时保留在线结果并给出明确 warning，不能静默产出错误拼图。
- 控制面板只显示状态（采集中/等待滚动/校准中/已恢复/请慢一些）、当前高度、 帧数、取消、完成，不显示大预览，并且不能进入最终图片。
- 完成时要等待正在进行的最后一次抓帧，避免长图漏掉末尾一段。

### 2.6 钉图

- 把截图/剪贴板图片作为无边框浮动窗口固定在桌面（niri floating 布局）。
- 滚轮缩放图片内容，`Ctrl+滚轮` 缩放窗口，拖动移动窗口。
- 快捷键：`c` 复制、`s` 保存、`0` 重置缩放、`q`/`Esc` 关闭。

### 2.7 系统托盘

- 图标状态：就绪（相机）、采集中（录制点）、异常（警告）。
- 右键菜单：区域截图、长截图/完成长截图（采集中动态切换文案）、钉图、 截图后保存开关、截图后复制开关、运行诊断、重启服务、退出。
- 必须导出传统 dbusmenu，保证在 niri/QuickShell 的托盘宿主里右键菜单不为空 （这是 Python 版本的已知坑，见第 7 节）。

### 2.8 后台服务与快捷键热路径

现有架构（重写时可以用更快的实现方式，但语义要保留）：

```
niri binds
  -> 本地可执行文件（由 niri 直接调用）
  -> Unix socket: $XDG_RUNTIME_DIR/pngshot/control.sock
  -> 常驻控制服务（不依赖 GUI 工具包，只负责调度）
  -> 拉起一次性 GUI 动作进程/窗口
```

- 控制服务用 JSON-over-Unix-socket 协议，至少支持 `ping`、`status`、 `action`（`region` / `long` / `pin-last`）、`shutdown`。
- `long` 动作是**开关式**的：服务发现当前已有一个 `long` 动作在跑时，不视为 busy，而是给活动进程发送完成信号（原实现用 `SIGUSR1`）。
- `region`/`long` 互斥（同一时间只能有一个选择器），`pin-last` 不互斥。
- 服务不可用时客户端要能自动拉起（优先走 systemd `--user` unit，失败则直接 spawn 常驻进程），全部失败才提示用户。
- 服务日志滚动写入 `~/.local/state/pngshot/service.log`。

### 2.9 systemd 与安装

- 提供 `pngshot.service`（控制服务）和 `pngshot-tray.service`（托盘）两个 systemd `--user` unit。
- 一键安装脚本：装系统依赖（仅缺失时用 `sudo`）、装源码/二进制、装 `pngshot`/`pngshotctl` 命令、装 desktop 入口和图标、装并启动两个服务、 尝试把默认快捷键安全写入用户的 niri `keybinds.kdl`。
- 快捷键自动配置规则：检测到任意冲突就整组不写、只提示手动配置；写入前备份 `keybinds.kdl.pngshot-backup`；写入后跑 `niri validate`，失败自动恢复备份； niri 正在运行时尝试自动 reload。
- 安装脚本幂等，可重复执行来更新。

### 2.10 CLI / 诊断

- `pngshot region` / `pngshot long` / `pngshot pin-last`，支持 `--no-save` `--no-copy`。
- `pngshotctl status` / `doctor`（检查依赖、Wayland、niri、grim 等价能力、 剪贴板、OCR、翻译后端、快捷键）/ `shortcuts`（含 `install`/`remove`）/ `restart` / `logs` / `tray`。

### 2.11 配置文件

`~/.config/pngshot/config.toml`，缺省时使用内置默认值，至少覆盖：

```toml
[llm]
provider = "opencode"        # 或未来扩展的其它后端
model = "opencode/deepseek-v4-flash-free"
target_lang = "简体中文"
timeout_s = 30
serve_port = 47823

[ocr]
engine = "tesseract"          # 或 "vision"
langs = "chi_sim+eng"
preprocess = true
upscale = 3.0
vision_model = "google/gemini-flash-latest"
vision_timeout_s = 30

[longshot]
poll_ms = 0
min_shift_px = 4
max_diff = 9.0
```

------

## 3. Rust 技术选型建议（请先验证再定稿）

目标是"编译型语言的启动速度 + 现有 Wayland/niri 生态兼容性"，建议方向：

- **GUI/覆盖层**：优先评估 `gtk4-rs` + `gtk4-layer-shell` 的 Rust 绑定， 这样可以继续用 layer-shell 精确控制覆盖层几何、复用 niri 下已验证可行的 合成方式；如果调研后发现启动开销仍然明显（GTK/GI 初始化），再评估 `smithay-client-toolkit`（直接对接 `wlr-layer-shell` 协议，不经过 GTK） 作为覆盖层/选区/长截图面板的更轻量替代，工具栏、标注、结果窗口等复杂交互 界面可以仍用 GTK4，只有"必须极致快"的首屏覆盖层单独轻量化。 **两条路线都要做启动耗时的真实测量再决定**，不要凭感觉选型。
- **截图抓取**：Wayland 下等价于现在调用的 `grim`，可以继续 shell 出去调用 `grim`，也可以评估直接用 `wlr-screencopy` 协议自己抓帧以降低单帧延迟 （尤其对长截图的连续抓帧速度收益明显）。
- **图像处理/拼接**：`image` crate 做基础像素操作，`rayon` 做并行化， 必要时上 SIMD（`wide` 或平台 intrinsics）加速行签名匹配和像素级校验， 这是长截图"效率更上一层楼"的关键落点。
- **OCR**：优先绑定/调用本地 `tesseract`（如 `leptess` 或直接 shell 出去用 现有 CLI 都可以，选一个启动开销更小的），视觉 OCR 分支继续走 HTTP 调用 外部模型服务。
- **托盘**：找 Rust 下能导出传统 dbusmenu 的 AppIndicator 绑定（如 `ksni` 或类似 crate），必须验证在当前 niri/QuickShell 托盘宿主下右键菜单 正常出现，不能只测图标显示。
- **IPC**：控制服务继续用 Unix domain socket，协议可以保持 JSON（`serde_json`） 以降低调试成本，也可以评估更紧凑的二进制协议，但要说明取舍。
- **异步运行时**：如果引入 `tokio`，注意控制服务本身要足够轻（不需要成为 一个大型异步应用），评估同步 std 实现是否已经够用。

请把最终选型、每个选型的备选方案和放弃理由写进新仓库的 `ARCHITECTURE.md`。

------

## 4. 长截图：具体要"更上一层楼"在哪

不是照搬 Python 版本算法翻译成 Rust，而是要在保留全部鲁棒性行为（第 2.5 节） 的前提下改进：

1. **抓帧吞吐**：现有 Python 版本单次 `grim` 约 30–40ms，评估直接走 `wlr-screencopy` 协议是否能显著降低单帧延迟和 CPU 开销。
2. **匹配算法性能**：行签名匹配 + 稀疏像素校验的双重匹配要用 Rust 的 零成本抽象和 SIMD 重写，目标是同规格基准（如 101 帧 900x700、每帧滚动 20px）的采集+拼接总耗时明显优于 Python 基线（原基线约 1.4~2.3 秒， 取决于是否半透明），并给出你自己环境下的新基准数据。
3. **内存**：canvas 增量拼接（不要每帧整体 `vstack`/重分配），关键帧压缩保留 策略要保持有界（原实现硬上限 48 MiB），可以评估更优的关键帧选择/压缩方式 进一步降低占用。
4. **准确性不能倒退**：固定区域检测（贴边、至少三次滚动确认、单边不超过 45% 视口）、半透明背景羽化（视口中心加权、动画区域保留最近帧而非平均残影） 这些行为要在新实现里写等价的合成测试用例，覆盖：向下滚动、向上滚动、 往返滚动、局部动画、断链后恢复、结尾在途帧、固定头尾、固定侧栏、 半透明背景。
5. 允许调整实现细节和内部数据结构，但**不允许为了让测试更容易通过而放宽 匹配阈值**（这是原项目明确踩过的坑）。

------

## 5. UI 重新设计要求

- 保留现有视觉语言：深色半透明表面、蓝色代表主要动作/选区、绿色代表运行中 状态、红色仅用于真实错误。

- 在此基础上做

  更精致的视觉打磨

  ：更讲究的间距/圆角/阴影层级、更克制的 动效（如选区吸附、工具栏淡入）、图标一致性，但拒绝：

  - 移动端风格的大卡片堆叠
  - 巨型标题、装饰性渐变
  - 任何让常驻内存/启动时间变大的"重客户端"化倾向

- 所有控件必须验证不会进入截图内容本身，且要适配窄选区和 niri 常见分栏宽度 （1/3、1/2、2/3），不能被裁切或遮挡。

- 不熟悉的图标要有 tooltip；常用动作用图标即可。

- UI 改动前要看一遍现有 Python 版本截图/CSS，确保新设计是"精致化"而不是 "推倒重来的另一套语言"。

------

## 6. 快捷键响应速度要求

- 明确目标：**从 niri 触发绑定命令，到覆盖层内容出现在屏幕上的端到端延迟**， 要有具体的毫秒级测量方法（比如在覆盖层 map 时打点、结合合成器侧时间戳）， 并写入 `ARCHITECTURE.md` 或 `PERFORMANCE.md`。
- 复用"常驻轻量控制服务 + 一次性动作进程"的思路，但重新评估是否值得让 覆盖层本身也常驻（如 idle 时不创建可见窗口，收到请求后立即 map）， 用实测数据决定，不要预设结论。
- 控制服务本身的 socket round-trip 要做到亚毫秒到个位数毫秒级别，这在 Rust 下应该是自然结果，但要有基准测试验证，不能只凭"Rust 应该更快"这种直觉。

------

## 7. 已知坑（Python 版本踩过的，Rust 版本要主动规避或验证不复现）

1. 覆盖层/结果窗口/钉图这类长生命周期窗口如果被 GUI 工具包当成"单实例"， 新请求会激活旧窗口导致新截图丢失——新实现要确认每次请求都是独立实例/ 独立窗口。
2. 标注模式下不能被"用户无输入超时"逻辑误关闭；焦点事件在 layer-shell 下 不总是可靠，看门狗要依据真实输入活动时间戳，且标注模式应豁免自动取消。
3. 托盘右键菜单为空：必须确认实际导出的菜单协议被当前 niri/QuickShell 托盘 宿主正确解析，只测图标显示是不够的。
4. 任何长截图 UI 布局改动都要检查其几何范围与实际采样矩形不重叠，否则控制 面板或高亮边框会被拼进最终图片。
5. 完成长截图时如果省略"等待在途抓帧 + 清空队列 + 处理最新帧"这一步，会 丢失末尾一段内容——这个收尾逻辑不能被"简化"掉。

------

## 8. 交付方式与工作流程

1. 先输出一份 `ARCHITECTURE.md` 方案（模块划分、crate 选型、进程模型、 长截图算法设计要点、性能目标和测量方法），确认后再开始写代码。
2. 按模块分阶段实现，建议顺序：
   1. 控制服务 + Unix socket 协议 + CLI 骨架（先把"能被 niri 调用"跑通）
   2. 区域截图 + 覆盖层 + 工具栏（先用现有粗糙 UI，功能对齐优先）
   3. 标注
   4. 长截图（抓帧 → 拼接 → 固定区域/羽化 → 离线全局校正）
   5. OCR + 翻译
   6. 钉图
   7. 托盘
   8. systemd unit + 安装脚本 + niri 快捷键自动配置
   9. UI 精致化打磨（在功能已对齐的基础上单独一轮视觉/交互优化）
3. 每个模块要有自动化测试（尤其长截图的合成回归测试，参考第 4 节的用例 列表），GUI/托盘/快捷键/长截图额外要有真机验收记录。
4. 每完成一个阶段，报告：实现了什么、测试结果、性能数据（尤其快捷键延迟和 长截图基准）、与原 Python 版本的行为差异（如果有，必须明确说明并给出 理由，不能是无意识的功能缺失）。

------

## 9. 现在请做的第一件事

阅读随附的原项目源码（重点：`pngshot/controller.py`、`pngshot/fastctl.py`、 `pngshot/shortcuts.py`、`pngshot/overlay/`、`pngshot/longshot/`、 `pngshot/services/`、`pngshot/pin/`、`pngshot/tray.py`、`install.sh`）， 确认你对上述功能清单和坑点的理解与源码一致，然后输出第 8 节第 1 步要求的 `ARCHITECTURE.md` 草案供确认。
